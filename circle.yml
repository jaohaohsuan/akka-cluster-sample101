machine:
  services:
    - docker

  java:
    version: oraclejdk8

  environment:
    SBT_VERSION: 0.13.11
    SBT_OPTS: "-Xms512M -Xmx1536M -Xss1M -XX:+CMSClassUnloadingEnabled -XX:MaxPermSize=256M"